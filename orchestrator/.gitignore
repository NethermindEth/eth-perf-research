.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
state/
*.rlp
*.jsonl
run-manifest.json
.omc/
target.yaml
# Local stack scratch — bind-mount targets / generated reports / per-host paths.
nethermind-data/
reports/
lab-genesis.json
# Demo runs (DAG-driven) write to *-demo dirs so prod state stays clean.
nethermind-data-demo/
state-demo/
target-demo.yaml
.env.demo
# Geth datadir + runtime files; keep state-geth/genesis.json (the committed config).
state-geth/datadir/
state-geth/geth.log
state-geth/jwt.hex
