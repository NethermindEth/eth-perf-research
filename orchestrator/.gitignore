.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store
state/
*.rlp
*.jsonl
run-manifest.json
.omc/
target.yaml
# Per-scenario host scratch (not under target.yaml.example).
target.yaml.mainnet-100m
target.yaml.phase2-mainnet

# Lockfile — left to local `uv lock` so PRs don't drag pinned graphs.
uv.lock

# Ablation harness — split into a follow-up PR after orchestrator core merges.
scripts/run_ablation.sh
scripts/summarize_ablations.py
ablation-runs/
# Local stack scratch — bind-mount targets / generated reports / per-host paths.
nethermind-data/
reports/
lab-genesis.json
# Demo runs (DAG-driven) write to *-demo dirs so prod state stays clean.
nethermind-data-demo/
state-demo/
target-demo.yaml
.env.demo
# Geth datadir + runtime files; keep state-geth/genesis.json (the committed config).
state-geth/datadir/
state-geth/geth.log
state-geth/jwt.hex
